# The dashboard pipeline is deterministic and does not require an AI API key.
# PORT controls the private local preview and spreadsheet upload server.
PORT=3000
DASHBOARD_AUTO_PUBLISH=true
DASHBOARD_PUBLISH_BRANCH=main

# Optional metadata used by deployment environments.
APP_URL="http://127.0.0.1:3000"
